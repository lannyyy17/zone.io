{
  "entities": {
    "NetworkSignal": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "NetworkSignal",
      "type": "object",
      "description": "Represents a network signal data point collected by the mobile app.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the network signal data point."
        },
        "latitude": {
          "type": "number",
          "description": "Latitude coordinate of the network signal measurement."
        },
        "longitude": {
          "type": "number",
          "description": "Longitude coordinate of the network signal measurement."
        },
        "signalStrength": {
          "type": "number",
          "description": "Signal strength measurement (RSSI) in dBm, can be negative."
        },
        "sessionId": {
          "type": "string",
          "description": "Reference to the Session during which this signal was recorded."
        },
        "timestamp": {
            "type": "number",
            "description": "Timestamp of when the signal was recorded as a unix epoch milliseconds."
        }
      },
      "required": [
        "id",
        "latitude",
        "longitude",
        "signalStrength",
        "sessionId",
        "timestamp"
      ]
    },
    "Session": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Session",
      "type": "object",
      "description": "Represents a data collection session initiated by a user on the mobile app.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the session."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who initiated this session."
        },
        "startTime": {
          "type": "number",
          "description": "Timestamp indicating when the session started as a unix epoch milliseconds."
        },
        "endTime": {
          "type": "number",
          "description": "Timestamp indicating when the session ended. Null if the session is still active."
        },
        "locationName": {
          "type": "string",
          "description": "Optional name provided by the user to identify the location where the session was recorded."
        }
      },
      "required": [
        "id",
        "userId",
        "startTime"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Zone.io application. Does NOT store authentication information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user (matches auth uid)."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "Display name of the user."
        }
      },
      "required": [
        "id",
        "email"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
     "/users/{userId}": {
        "schema": {"$ref": "#/entities/User"},
        "description": "Stores user profile information."
      },
      "/users/{userId}/sessions/{sessionId}": {
        "schema": {"$ref": "#/entities/Session"},
        "description": "Stores data collection sessions for a user."
      },
      "/users/{userId}/network_signals/{signalId}": {
        "schema": {"$ref": "#/entities/NetworkSignal"},
        "description": "Stores all network signal data points for a user, across all sessions."
      }
  }
}
